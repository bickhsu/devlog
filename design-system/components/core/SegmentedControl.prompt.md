# SegmentedControl

Segmented control with a sliding indicator. Used for Copy Context Range.

```jsx
<SegmentedControl options={['Today','Last Week','All']} defaultValue="Today" onChange={setRange} />
```
