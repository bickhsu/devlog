# Logo

DevLog wordmark — a terminal caret block + mono "devlog". Use in top bars, splashes, settings.

```jsx
<Logo />                       {/* caret + devlog */}
<Logo variant="bracket" />     {/* [ devlog ] */}
<Logo variant="mark" blink />  {/* caret only, blinking */}
```
