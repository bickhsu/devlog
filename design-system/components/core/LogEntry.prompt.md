# LogEntry

A work-log entry in the thread. Hover reveals time + context + a copy affordance.

```jsx
<LogEntry status="done" title="BuildingSchemaAdapter" time="11:04"
  context="/metai/cad-agent/adapter" body="Set up the repo…" />
```

status: done · progress · failed · note (accent rail). title is an optional bold first line.
