# Button

Primary action button. Mono label, hairline border, tactile 0.97 press.

```jsx
<Button variant="primary" icon="plus">New context</Button>
<Button variant="ghost" icon="copy">Copy</Button>
```

variants: primary · secondary (default) · ghost · danger. sizes: sm · md · lg. Props: icon, iconRight (Lucide names), disabled.
