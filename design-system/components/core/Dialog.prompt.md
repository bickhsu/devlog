# Dialog

Centered modal on a dim scrim. Used for Create Context confirms. Positions absolutely within the nearest positioned ancestor.

```jsx
<Dialog title="Create Context" icon="circle-plus"
  confirmLabel="OK" onConfirm={create} onClose={cancel}>
  <code>/metai/cad-agent/adapter/migration-test/test1</code>
</Dialog>
```
