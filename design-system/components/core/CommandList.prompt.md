# CommandList

The "/" command palette — a flat, filterable list of every context path. Drive activeIndex with arrow keys.

```jsx
<CommandList paths={allPaths} query={typed} activeIndex={i}
  onHover={setI} onSelect={switchContext} />
```
