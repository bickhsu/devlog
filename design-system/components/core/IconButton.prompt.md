# IconButton

Square icon-only button with a faint hover backing. Top-bar / toolbar actions.

```jsx
<IconButton icon="panel-left-open" title="Toggle sidebar" />
<IconButton icon="settings" active title="Settings" />
```

Pass title for tooltip + aria-label. active gives an accent, filled state.
