{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the DevLog main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
